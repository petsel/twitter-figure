/**
 *  poor men's module system root - but is considered to be sufficient enough in oder to solve the task.
 */
(function (global) {

  global.tf = {

  //util      : {},
    model     : {},
    view      : {},
    presenter : {}
  };

//return global.tf;

}(window || global || this));
